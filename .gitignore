#visual studio related
*.filters
*.iobj
*.ipdb
*.ipch
*.obj
*.db
*.o
*.exe
*.ilk
*.pdb
*.tlog
*.idb
*.suo
*.log
*.pgn

#misc
*.tar
*.tar.gz


#directories
x64/*
log/*
packages/*
.vs/*
build/*
build-*/
haVoc/*
.vscode/*
b/

# Measurement output: logs, PGNs and optimiser state. Machine-specific and
# large; results that matter belong in docs, not in the tree.
testruns/

# Python bytecode from scripts/
__pycache__/
*.pyc

# Networks are distributed from the release page, not from git: one ~20 MB blob
# per training iteration would make the repository permanently expensive to
# clone. nets/default.txt pins the hash, which is what makes one reproducible.
*.nnue
